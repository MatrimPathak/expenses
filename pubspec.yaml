name: expenses
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
    sdk: ">=2.19.6 <3.0.0"

dependencies:
    cupertino_icons: ^1.0.2
    dynamic_color: ^1.6.6
    firebase_core: ^2.16.0
    flutter:
        sdk: flutter
    intl:
    gsheets:
    provider:
    telephony:

dev_dependencies:
    flutter_lints: ^2.0.0
    flutter_test:
        sdk: flutter

flutter:
    uses-material-design: true
